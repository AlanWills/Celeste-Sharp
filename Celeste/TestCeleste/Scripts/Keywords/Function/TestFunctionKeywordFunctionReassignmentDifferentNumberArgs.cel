scoped firstVariable
scoped secondVariable

scoped function firstFunc(input,output)
	output = input
end

scoped function secondFunc(input,input2,output)
	output = input + input2
end

firstFunc(10,firstVariable)

firstFunc = secondFunc

firstFunc("Test","Reassignment",secondVariable)